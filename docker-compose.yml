version: '3.8'

services:
  # Aplicação AdonisJS
  app:
    build:
      context: .
      dockerfile: Dockerfile
      target: production
    container_name: p-pass-app
    restart: unless-stopped
    ports:
      - "${PORT:-3333}:3333"
    environment:
      NODE_ENV: production
      PORT: 3333
      HOST: 0.0.0.0
      APP_KEY: ${APP_KEY}
      LOG_LEVEL: ${LOG_LEVEL:-info}
      DB_HOST: postgres
      DB_PORT: 5432
      DB_USER: ${DB_USER:-postgres}
      DB_PASSWORD: ${DB_PASSWORD:-postgres}
      DB_DATABASE: ${DB_DATABASE:-p_pass}
    depends_on:
      postgres:
        condition: service_healthy
    networks:
      - p-pass-network
    volumes:
      - ./build:/app/build

  # Banco de dados PostgreSQL
  postgres:
    image: postgres:16-alpine
    container_name: p-pass-postgres
    restart: unless-stopped
    environment:
      POSTGRES_USER: ${DB_USER:-postgres}
      POSTGRES_PASSWORD: ${DB_PASSWORD:-postgres}
      POSTGRES_DB: ${DB_DATABASE:-p_pass}
    ports:
      - "${DB_PORT:-5432}:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    networks:
      - p-pass-network
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${DB_USER:-postgres}"]
      interval: 10s
      timeout: 5s
      retries: 5

networks:
  p-pass-network:
    driver: bridge

volumes:
  postgres_data:
    driver: local

